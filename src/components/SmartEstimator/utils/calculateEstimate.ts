// @deprecated Inactive Phase 1 placeholder. Production estimates use calculateIntegratedEstimate and deterministic category fallbacks.
import { defaults,pricing } from '../data/estimatorPricing';import type { Data,Estimate } from '../types/estimator'
const money=(n:number)=>`$${Math.round(n/25)*25}`
export function calculateEstimate(d:Data):Estimate{const p=pricing[d.service||'Other']||{hours:defaults.hours,materials:defaults.materials};let m=(d.projectType==='Commercial'?defaults.commercial:1)*(d.category==='Emergency Service'||d.urgency==='Emergency'?defaults.emergency:1)*(defaults.access[d.access]||1)*(d.location==='Exterior'?1.1:1)*(d.floor==='3+'?1.12:1);if(d.matching)m*=1.08;if(d.materials)m*=.82;const q=Math.max(1,Number(d.quantity)||1);m*=Math.min(q,3);const hrs=p.hours.map(v=>v*m) as [number,number],mat=p.materials.map(v=>v*m) as [number,number],labor=hrs.map(v=>v*defaults.rate) as [number,number],total=[Math.max(defaults.minimum,labor[0]+mat[0]),labor[1]+mat[1]];let score=0;if(d.description.length>40)score++;if(d.quantity)score++;if(d.dimensions)score++;if(d.photos.length)score++;if(d.city&&d.state)score++;if(d.access&&d.access!=='Unknown')score++;if(d.service==='Other'||d.urgency==='Emergency')score--;const confidence=score>=5?'Strong':score>=3?'Moderate':'Preliminary';return{laborHours:`${Math.round(hrs[0])}–${Math.round(hrs[1])} hours`,labor:`${money(labor[0])}–${money(labor[1])}`,materials:`${money(mat[0])}–${money(mat[1])}`,total:`${money(total[0])}–${money(total[1])}`,duration:hrs[1]<=8?'Same day to 2 days':hrs[1]<=24?'2–4 days':'Project timing to be confirmed',confidence,considerations:['Site conditions and concealed damage may change scope.','Material selection, access, permits, and matching finishes may affect final pricing.']}}
