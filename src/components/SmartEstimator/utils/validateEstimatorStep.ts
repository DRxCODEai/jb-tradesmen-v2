import type { Data } from '../types/estimator'
export function validate(step:number,d:Data){if(step===0&&!d.projectType)return'Select a project type to continue.';if(step===1&&!d.category)return'Select a service category to continue.';if(step===2&&!d.service)return'Tell us which service best matches your project.';if(step===3&&d.description.trim().length<12)return'Add a brief project description so we can create a planning range.';if(step===4&&(!d.city||!d.state||!d.propertyType))return'Enter your property type, city, and state.';if(step===6){if(!/\S+@\S+\.\S+/.test(d.email))return'Enter a valid email address.';if(d.phone.replace(/\D/g,'').length<10)return'Enter a valid phone number.';if(!d.consent)return'Confirm that you understand this is a preliminary estimate.'}return''}
