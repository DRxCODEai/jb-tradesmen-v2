import type { Data } from './types/estimator';export default function EstimatorSummary({data}:{data:Data}){const items=[['Project type',data.projectType],['Category',data.category],['Service',data.service],['Location',data.city&&data.state?`${data.city}, ${data.state}`:''],['Urgency',data.urgency],['Photos',data.photos.length?`${data.photos.length} added`:'']];return <aside className="smart-summary"><h2>Your project</h2>{items.filter(([,v])=>v).map(([k,v])=><p key={k}><span>{k}</span><strong>{v}</strong></p>)}</aside>}
